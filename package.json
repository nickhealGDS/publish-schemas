{
  "name": "publish-schemas",
  "scripts": {
    "release": "semantic-release --plugins 'semantic-release-export-data' --dry-run"
  },
  "devDependencies": {
    "semantic-release": "^24.2.7",
    "semantic-release-export-data": "^1.1.1"
  }
}
